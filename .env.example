# Copy these into your host's Environment Variables (Vercel: Project → Settings →
# Environment Variables; Netlify: Site settings → Environment variables).
# Do NOT commit a real key. Get one at https://console.anthropic.com

ANTHROPIC_API_KEY=
MODEL=claude-haiku-4-5-20251001

# Optional: set to 1 ONLY while debugging to surface the real upstream error in
# the chat reply (e.g. to see a 401 bad-key or 400). Leave unset in production.
DEBUG_ASSISTANT=
