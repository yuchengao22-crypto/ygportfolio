# Dependencies
node_modules/

# Secrets — never commit real keys (.env.example is safe and IS committed)
.env
.env.local
.env*.local

# OS / editor cruft
.DS_Store
*.log

# Local tooling / preview config (not needed for deploy)
.claude/
.vercel/
